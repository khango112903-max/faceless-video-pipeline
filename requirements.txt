# --- Script Generation ---
google-generativeai>=0.5.0
requests>=2.31.0

# --- Voice (TTS) ---
git+https://github.com/suno-ai/bark.git
scipy
numpy
# NOTE: We do NOT pin numpy<2 here — Kaggle's pre-installed scipy (needed by
# Bark) requires numpy 2.x. SadTalker's numpy 1.x incompatibilities are
# instead patched directly in its source code — see src/avatar.py's
# _patch_numpy2_incompatibilities().

# --- Subtitles ---
openai-whisper

# --- Video / Animation ---
# manim  # commented out: needs system-level libs (cairo/pango), unreliable on Colab.
#          Fallback visuals use PIL + moviepy instead (see src/visuals.py)
moviepy
Pillow
decorator==4.4.2
# ^ pinned: newer decorator versions break moviepy 1.0.3's fps handling,
#   causing "TypeError: must be real number, not NoneType" on export.

# --- Audio/Video processing ---
ffmpeg-python

# --- Avatar Scene (SadTalker) — optional, only needed if using avatar_photo ---
huggingface_hub
opencv-python
librosa==0.9.2
numba
face_alignment==1.3.5
imageio
imageio-ffmpeg
resampy
pydub
kornia==0.6.8
yacs
scikit-image
basicsr>=1.4.2
facexlib>=0.2.5
gfpgan
safetensors

# --- Utilities ---
python-dotenv
tqdm
