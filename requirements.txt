# --- Script Generation ---
google-generativeai>=0.5.0
requests>=2.31.0

# --- Voice (TTS) ---
git+https://github.com/suno-ai/bark.git
scipy
numpy

# --- Subtitles ---
openai-whisper

# --- Video / Animation ---
# manim  # commented out: needs system-level libs (cairo/pango), unreliable on Colab.
#          Fallback visuals use PIL + moviepy instead (see src/visuals.py)
moviepy
Pillow
decorator==4.4.2
# ^ pinned: newer decorator versions break moviepy 1.0.3's fps handling,
#   causing "TypeError: must be real number, not NoneType" on export.

# --- Audio/Video processing ---
ffmpeg-python

# --- Utilities ---
python-dotenv
tqdm
